[DEFAULT]
sources = %(here)s/db/migrations
verbosity = 1

# Database URL is supplied at runtime from db_connection.build_yoyo_database_url().
# CLI example:
#   yoyo apply --database "$DATABASE_URL" ./db/migrations
